# Local dev template — same Postgres user/password/port as docker-compose.yml (host 5433).
# Safe to commit: not a secret, only matches the default local stack. Use `.env` (gitignored)
# for anything different; never commit production or cloud DATABASE_URLs here.
DATABASE_URL=postgresql+psycopg2://eventmanager:eventmanager@127.0.0.1:5433/eventmanager
